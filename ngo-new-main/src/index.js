// Design handoff (Claude Design) takes over the frontend via public/index.html.
// The UMD React + Babel standalone scripts in public/ handle all rendering.
// CRA bundle is intentionally left empty to avoid mounting conflicts on #root.

export {};
